services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: invoice_generator
    ports:
      - "5432:5432"
    volumes:
      - invoice_pg:/var/lib/postgresql/data

volumes:
  invoice_pg:
